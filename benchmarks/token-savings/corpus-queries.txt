# Fixed query set for the PINNED benchmark corpus (benchmarks/token-savings/corpus/).
# One doc-lookup question per line; '#' lines ignored. Do NOT edit these two files
# independently — the published number in src/trovex/_benchmark.json is
# (corpus + queries + model). Re-running `run.py --fixed` against this pair
# reproduces src/trovex/_benchmark.json exactly.

how do I install acme notes
set the data directory and listen port
create and revoke an api token
run it in production behind a reverse proxy
where are notes stored on disk
how does the query cache get invalidated
how does search ranking and reranking work
does it work offline without an api key
how do I back up and move to a new machine
how do database migrations run
