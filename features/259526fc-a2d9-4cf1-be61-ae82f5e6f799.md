# 259526fc-a2d9-4cf1-be61-ae82f5e6f799

## Team : trovex-backend (trovex)
## Branch : fix/cast-chunk-followup-nits (from dev)
## Status : 🔵 SUBMITTED

## 1. Product Brief

### Acceptance Criteria
_(untyped ticket — no acceptance criteria)_

## 2. Root cause & decisions

ROOT_CAUSE: cAST code-chunking (9299f37d) shipped with 3 non-blocking review notes unactioned — byte-complete no-loss/no-dup and ratchet (distinct heading_paths across nested recursion) were only manually verified, not regression-tested, and the intra-symbol duplicate-heading_path behavior was documented but not framed against future misreads as a regression.

DECISION: Added two regression tests to tests/test_chunking_code.py — (1) byte-complete no-loss/no-dup across all 4 v1 langs (python/go/rust/typescript) x 3 token budgets (1/5/450), comparing whitespace-normalized reconstructed text to source; (2) a ratchet test proving the iterative stack-walk never lets one oversized sibling symbol's chunks bleed into another's heading_path or text. Strengthened the existing by-design comment at the labeled-node split branch in chunking_code.py to explicitly name intra-symbol heading_path duplication as the ONLY source of duplicate paths and point at the new ratchet test for the invariant that actually matters (distinct symbols never collide).

REJECTED_ALTERNATIVE: A blanket "no duplicate heading_path anywhere in a file" assertion — traced through the merge algorithm and confirmed structurally impossible whenever a node is oversized enough to require recursion (any oversized node necessarily emits >=2 chunks under its own path before the frame drains). Asserting global uniqueness would have been testing behavior the algorithm cannot and should not provide; the sibling-isolation invariant is the correct one to lock in.

Scope: test-only + one comment; no retrieval/write/auth/schema surface touched. make test green (397 passed), ruff clean.

## review-trovex verdict: SHIP
Diff is test-only (tests/test_chunking_code.py) + one strengthened comment (src/trovex/chunking_code.py) in the cAST code-chunker. No retrieval, write, auth, schema, or privacy-default surface touched — none of the review-backend checklist branches (recall integrity, write-side data integrity, reserved source id, auth/secrets, best-effort-vs-error, local-first defaults, schema/embed-dim, token-efficiency) apply. make test green (397 passed), ruff clean. No findings.

## 3. Files changed

```
src/trovex/chunking_code.py |  7 +++++
 tests/test_chunking_code.py | 72 +++++++++++++++++++++++++++++++++++++++++++++
 2 files changed, 79 insertions(+)
```

## 4. QA Log

_(no review round yet)_

## 5. Timeline


---
_Auto-assembled by the niwa scribe from the Q&A gate. Task `259526fc-a2d9-4cf1-be61-ae82f5e6f799`._
