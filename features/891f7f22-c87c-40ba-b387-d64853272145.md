# 891f7f22-c87c-40ba-b387-d64853272145

## Team : trovex-backend (trovex)
## Branch : fix/mcp-pin-lt2 (from dev)
## Status : 🔵 SUBMITTED

## 1. Product Brief

### Acceptance Criteria
_(untyped ticket — no acceptance criteria)_

## 2. Root cause & decisions

ROOT_CAUSE: `pyproject.toml` pinned `mcp>=1.2` with no upper bound. `mcp` 2.0.0 (released upstream) dropped/restructured `mcp.server.fastmcp` and `mcp.server.transport_security`; uv resolves 2.0.0 for any fresh install (the dev worktree's `uv.lock` was already pinned to 1.28.1 pre-existing, so this only surfaced on a *fresh* `uv tool install trovex`, not in CI/dev). Result: every fresh install crashed at boot — `ModuleNotFoundError: No module named 'mcp.server.fastmcp'` (`mcp_app.py:14-15`).

DECISION: pin `mcp>=1.2,<2` (v1 stays supported; migration to mcp 2.x is a separate, future ticket — not bundled here). Regenerated `uv.lock` (resolves back to 1.28.1, unchanged from before this broke). Ported the root `Makefile` (identical to the cto spec already reviewed/merged via #724) onto this `dev`-cut branch, since `dev` doesn't have it yet and `check_kind=make` needs a runner to resolve.

REJECTED_ALTERNATIVE: migrating the two imports to mcp 2.x's new module layout now — out of scope for a P0 hotfix; wider blast radius (unverified 2.x API surface) when a one-line pin fully restores the working state.

VERIFICATION: built the wheel off this branch, installed into a throwaway clean venv, confirmed `pip show mcp` resolves 1.29.0 (<2), re-ran the two previously-crashing imports directly (clean), and booted `trovex serve` end-to-end (isolated data dir, no concurrent instance) to `GET / -> 200 OK`. `make test`: 372 passed, ruff clean.

## review-backend verdict: SHIP
dependency pin + lockfile regen + build-tooling file only, no retrieval/write/auth/schema/privacy surface touched, no findings.

## 3. Files changed

```
Makefile       | 5 +++++
 pyproject.toml | 2 +-
 uv.lock        | 2 +-
 3 files changed, 7 insertions(+), 2 deletions(-)
```

## 4. QA Log

_(no review round yet)_

## 5. Timeline


---
_Auto-assembled by the niwa scribe from the Q&A gate. Task `891f7f22-c87c-40ba-b387-d64853272145`._
