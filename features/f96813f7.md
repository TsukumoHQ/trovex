# f96813f7

## Team : trovex-backend (tsukumo)
## Branch : feat/flagship-ssot-default (from main)
## Status : 🔵 SUBMITTED

## 1. Product Brief

### Acceptance Criteria
_(untyped ticket — no acceptance criteria)_

## 2. Root cause & decisions

# P2b — flagship default routes to the SSOT tier (task f96813f7)

## ROOT_CAUSE / GOAL
After P2a (source_id partitioning), the owned SSOT (~462 'trovex' docs) and the
cold file-indexed artifacts (~3900 across code/tsukumo/wraith/…) are already
separate vec0 shards. What remained: make the DEFAULT flagship query precise —
scan only the ssot tier, not the whole store — with the cold corpus opt-in.

## DECISION (cto-confirmed matrix)
`_resolve_source` routing:
- `source='*'` (either level) → None → scan EVERY partition (opt-in escape hatch,
  reaches the cold corpus; keeps the source=* contract the 684035ff-allsources
  review forced).
- nothing set (unpinned) → `TROVEX_SOURCE_ID` = the ssot tier (the precise
  default — tiny k, zero cross-source noise). **Intentional contract change:
  unpinned is no longer whole-store.**
- explicit/pinned `source=X` → partition X.
- boot / flagship-resume → already pass `source_ids=['trovex']` explicitly (P2a),
  unaffected.

One-line code change (route unpinned→trovex, None only for '*'); the two call
sites (flagship `trovex`, `trovex_search`) inherit it. Docstrings updated to say
the unpinned default is your SSOT records; `*` searches every source incl. cold.

## VALIDATION
- test_mcp_contract: `test_scope_defaults_to_ssot` (unpinned → 'trovex', flipped
  from whole-store); `test_star_means_all_sources` still None; unknown/pin tests
  intact. 15 passed.
- New `test_boot_never_returns_cold_tier_but_explicit_source_reaches_it`: a cold
  'code' artifact matching BOOT_QUERY verbatim never enters the boot pack (tier
  scoping, not tags/score, excludes it); an explicit source=['code'] query still
  reaches it.
- Gate green: ruff clean, pytest 359 passed, brand clean.
- Live copy: unpinned default → trovex-only, scans 462 (not 4355); `source='*'`
  spans 4 sources. DoD met.

## review-trovex verdict: SHIP

review-trovex: ✅ ship — 3 files (1 src + 2 test) — gate green (ruff+pytest+brand),
flagship default now ssot-precise, cold opt-in, boot tier-scoped, source=* contract
intact, live-validated. User-facing default-semantics change (unpinned→ssot) —
cto-confirmed → PR-to-cto (redeploy carries it).

## 3. Files changed

```
src/trovex/mcp_app.py       | 30 ++++++++++++++++++++++--------
 tests/test_active_memory.py | 43 +++++++++++++++++++++++++++++++++++++++++++
 tests/test_mcp_contract.py  |  9 +++++----
 3 files changed, 70 insertions(+), 12 deletions(-)
```

## 4. QA Log

_(no review round yet)_

## 5. Timeline


---
_Auto-assembled by the niwa scribe from the Q&A gate. Task `f96813f7`._
