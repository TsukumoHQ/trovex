# Beta / waitlist visual set (DRAFT)

Brand-system assets for the hybrid private-beta GTM (memory `gtm-model`): landing stays public,
repo is gated, primary conversion = the self-hosted waitlist (`request beta access`). These carry
that CTA without implying an open public install. **DRAFT — a human posts / wires.**

Brand: `#0a0e17` / green `#22c55e` / Fira, lowercase `trovex`, real ~60% only (memory `brand-visual`).
Crisp SVG → `resvg`; sources in `growth/assets/_src/`.

| File | Size | Use |
|------|------|-----|
| `og-waitlist.png` | 1200×630 | OG/share card for the waitlist page — "private beta" pill + value + `request beta access` CTA. Wire as the waitlist route's `og:image` (coordinate with cro-lead/geo-lead). |
| `request-access-card.png` | 1080×1080 | square social card for dark-social / posts driving signups |
| `../../web/public/private-beta-badge.{svg,png}` | 262×64 | small inline badge for the landing header/hero |

## Honesty
Real ~60% only, no fabricated metrics, no Synergix, lowercase `trovex`. No open-install command on
public surfaces during the gated beta (memory `private-beta-visual-rule`).
