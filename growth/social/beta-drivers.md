# Beta-driver social drafts — private-beta waitlist (DRAFT)

> **Status: DRAFT — do not post. Human fires this (no X/LinkedIn creds in this env).**
> GTM = hybrid private beta (memory `gtm-model`): landing public, repo private, CTA =
> **request beta access**. These drive waitlist signups (the primary conversion + consulting
> lead). **Do NOT** use install-now copy or link the github repo (it's private now). Only link
> trovex.dev with the UTM tags below. Only real number: ~60%. No fabricated proof, no
> "join 500 devs" — it's an early private beta. Lowercase `trovex`. Passed copy-gate.

## UTM links (per `growth/analytics/geo-attribution.md`, social owns social links)
- X: `https://trovex.dev/?utm_source=x&utm_medium=social&utm_campaign=beta-waitlist#waitlist`
- LinkedIn: `https://trovex.dev/?utm_source=linkedin&utm_medium=social&utm_campaign=beta-waitlist#waitlist`
- Put the link in a REPLY (X) / FIRST COMMENT (LinkedIn), not the post body.

---

## X — beta-invite thread

**1/ hook**
> i'm opening a small private beta for trovex: it stops your coding agent rereading the repo's
> docs every session to guess which `.md` is current. one canonical answer instead of three
> stale files — about ~60% fewer tokens on doc lookups.
>
> if you run agents on a doc-heavy repo, request access 👇

**2/**
> the problem it kills: every session, your agent rereads a runbook, an old duplicate, a
> postmortem — to answer a question it already answered yesterday. you pay for the guessing,
> every session, every agent, every teammate.

**3/**
> what beta access gets you: point it at your repo, it indexes the markdown, and your agent
> gets back the one current doc per query (path:line + a freshness marker), section-level.
> runs locally — SQLite + ONNX, no cloud, no keys.

**4/**
> it's an early private beta, so it's hands-on and small on purpose — i want real feedback from
> people running agents daily, not a vanity signup count.

**5/ CTA (link in this reply)**
> request access: [trovex.dev beta link w/ UTM]
> if you're rolling agents out across a team, mention it — that's the case i most want in the
> beta.

## X — single-post variants (use standalone, space them out)

**A**
> opening a private beta: trovex serves your coding agent one canonical doc per query instead
> of letting it reread the repo to guess. ~60% fewer tokens on doc lookups. request access if
> you run agents on a doc-heavy repo. [link in reply]

**B**
> not launching publicly yet — running a small private beta first. if your agent burns tokens
> rereading the same docs every session, that's exactly the problem trovex fixes. want in?
> [link in reply]

---

## LinkedIn — beta-invite post

> I'm opening a small private beta for trovex, and I'd rather have ten people who run agents
> daily than a thousand signups.

> The problem it solves is a quiet one. Your coding agent rereads several `.md` files every
> session to work out which is the current source of truth, then answers from a guess. You pay
> for that on every session, every agent, every teammate. trovex serves the one canonical doc
> per query (with a freshness marker, section-level) instead. About ~60% fewer tokens on doc
> lookups, same answers. It runs locally.

> The beta is deliberately small and hands-on. I want to watch it work on real, messy,
> doc-heavy repos and fix what doesn't. If your team runs agents across a repo and the
> doc-rereading tax is a daily line item, that's exactly the case I want in.

> Request access in the comments. (And if "running agents well across a team" is the actual
> problem, that's the conversation I'm here for.)

---

## Posting notes
- Link → X reply / LinkedIn first comment, with the UTM above. Never the body.
- Frame is "request access", never "install now" / "star the repo" (repo is private during beta).
- Reply fast in the first hour; beta invites convert on the founder showing up.

## Self-audit
- [x] Private-beta / "request access" framing throughout; no install-now, no github repo link.
- [x] UTM-tagged trovex.dev links per analytics convention (x/linkedin · social · beta-waitlist).
- [x] Only ~60% claimed; no fabricated signups/proof. No banned words. Lowercase `trovex`.
- [x] Consulting surfaces once, end-weighted. No Synergix.
